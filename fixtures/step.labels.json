{
  "url": "https://step-shoe-store-production.up.railway.app/",
  "note": [
    "Ground truth, written by reading step.html and step-mega.js — NOT by running",
    "the tool and blessing what it found. That distinction is the whole value of",
    "this file: a corpus built from the tool's own output can only ever score 100%.",
    "",
    "Several components are BUILT BY JAVASCRIPT after load (mega-nav's items, the",
    "hero slides, the deal tabs, the FAQ accordion, the branch-locator detail).",
    "Their selectors are taken from the template literals in step-mega.js, so the",
    "labels describe the page as a user meets it rather than as it is served.",
    "",
    "`hidden: true` marks a component that exists only while opened. The survey",
    "cannot see it, and a scorer that counts it as a miss is measuring the wrong",
    "thing — it is scored separately, under `openable`.",
    "",
    "`type: \"none\"` pins something that must NOT be flagged — a near-miss built",
    "into the page on purpose (a lone button whose class says 'modal', a layout",
    "table, a price slider). Never scored for recall; a detection matching one is",
    "a CONFIRMED false positive.",
    "",
    "Tab strips are labelled `tabs`. Until stage 4 restores the tabs/menu split,",
    "both harnesses accept `menu` for them and print the count owed.",
    "",
    "`single: true` on an accordion marks a lone disclosure — one trigger, one",
    "panel. The type stays accordion by decision; the flag is for stage 4."
  ],
  "components": [
    {
      "type": "menu",
      "root": "#megaNav",
      "why": "Seven top-level triggers, each opening a panel of links. One menu, not seven. Rooted on ul.mega-nav__list (id megaNav), NOT on the .mega-nav <nav> that wraps it: u1.fix.menu reads the root's OWN CHILDREN, and the nav has exactly one child — this list. Rooted on the wrapper it is a menu of one item. That is the engine's contract, not a preference, so the wrapper was simply the wrong label.",
      "fields": {
        "items": ".mega-nav__trigger,.mega-panel__links>li>a",
        "triggers": ".mega-nav__trigger",
        "submenus": ".mega-panel"
      },
      "builtByJs": true
    },
    {
      "type": "tabs",
      "root": ".finder__tabs",
      "why": "Five sibling controls over five panels that swap in place — the textbook tab strip. Was relabelled `menu` when the collapse was decided; restored to `tabs` for the stage-4 split. Harnesses accept `menu` for it meanwhile, counted and warned.",
      "fields": {
        "items": ".finder__tab",
        "submenus": ".finder__panel"
      }
    },
    {
      "type": "tabs",
      "root": "#dealTabs",
      "why": "Six sibling controls over ONE re-rendered region. A strip over one region is still the shape, and the already-selected one belongs to it. Restored to `tabs`, menu accepted until stage 4.",
      "fields": {
        "items": ".tab-bar__btn",
        "submenus": "#dealGrid"
      },
      "builtByJs": true
    },
    {
      "type": "tabs",
      "root": "#faqTabs",
      "why": "The same strip shape again, over the accordion below it. Restored to `tabs`, menu accepted until stage 4.",
      "fields": {
        "items": ".tab-bar__btn",
        "submenus": "#faqPanel"
      },
      "builtByJs": true
    },
    {
      "type": "accordion",
      "root": "#faqPanel",
      "why": "The FAQ items under the tab strip: several sibling header buttons, each opening its answer in place. Built by mega.js — invisible to the jsdom harness until its renderer list included faqTabs/faq, and reported as a false positive for exactly that reason. It was a real accordion all along; the corpus was short.",
      "fields": {},
      "builtByJs": true
    },
    {
      "type": "carousel",
      "root": ".hero-carousel",
      "why": "aria-roledescription=carousel, prev/next, dots, and a 6.5s auto-advance.",
      "fields": {
        "slide": ".hero-slide",
        "prevButton": "#heroPrev",
        "nextButton": "#heroNext"
      },
      "builtByJs": true
    },
    {
      "type": "form",
      "root": "[data-finder-form]",
      "why": "Five of them, one per finder panel. A selector that matches all five is correct here.",
      "fields": {
        "inputField": ".finder__control"
      },
      "matches": 5
    },
    {
      "type": "form",
      "root": "#subscribeForm",
      "why": "A real <form>: five labelled fields, a required consent checkbox, a submit button. Flagged by the tool and unlabelled until the precision metric surfaced it — the finding was about the corpus, not the detector.",
      "fields": {
        "inputField": ".subscribe__input"
      }
    },
    {
      "type": "table",
      "root": "#tierBenefitsTable",
      "why": "The membership-tiers comparison. Was rooted on bare `.data-table`, which matches whichever of the page's four data tables happens to come first — each table now carries its own id and its own label.",
      "fields": {}
    },
    {
      "type": "table",
      "root": "#sizeChartTable",
      "why": "The size conversion chart — a real <table> with scope'd headers and a caption.",
      "fields": {}
    },
    {
      "type": "table",
      "root": "#compareSpecTable",
      "why": "The four-flagship spec comparison — a real <table>.",
      "fields": {}
    },
    {
      "type": "table",
      "root": "#locatorDetail",
      "why": "The branch detail pane renders an opening-hours <table> per selected store. Rooted on the pane because the table inside is rebuilt on every selection.",
      "fields": {},
      "builtByJs": true
    },
    {
      "type": "tabs",
      "root": "#locatorList",
      "why": "The branch list: sibling buttons with aria-current, pressing one re-renders the fixed detail pane beside it — behaviourally identical to dealTabs (panel swapped in one place = tabs). Unlabelled until 4.3's swap-in-place rule started seeing it; the detection was right, the corpus was short (corpus change, not code).",
      "fields": {
        "tab": ".locator__item"
      },
      "builtByJs": true
    },
    {
      "type": "dialog",
      "root": ".search-panel",
      "why": "role=dialog aria-modal=true, opened from the Search button.",
      "fields": {
        "trigger": "#searchOpen"
      },
      "hidden": true
    },
    {
      "type": "dialog",
      "root": ".cart-drawer",
      "why": "A drawer is a modal by behaviour: it covers the page and traps focus.",
      "fields": {
        "trigger": "#cartToggle"
      },
      "hidden": true
    },
    {
      "type": "dialog",
      "root": "#sizeModal",
      "why": "The size-guide modal, opened from two [data-modal-open] buttons. Flagged by the tool and unlabelled until the precision metric surfaced it.",
      "fields": {
        "trigger": "[data-modal-open=\"sizeModal\"]"
      },
      "hidden": true
    },
    {
      "type": "breadcrumb",
      "root": "nav[aria-label=\"Breadcrumb\"]",
      "why": "A trail: links with a separator between each pair, in text smaller than the body. Labelled `menu` until the rule was decided — it is navigation, but it is not a menu, and it now has a type of its own.",
      "fields": {}
    },

    {
      "type": "menu",
      "root": "#auditAccountList",
      "why": "My account: a button opening a list of five real links, OUTSIDE any <nav>. A dropdown of links is a menu wherever it lives — the footer demotion and the nav requirement must not eat it.",
      "fields": {
        "trigger": "#auditAccountBtn"
      },
      "hidden": true
    },
    {
      "type": "menu",
      "root": "#auditDropdownMenu",
      "why": "A Bootstrap-style dropdown: data-toggle=dropdown, no aria-controls, real hrefs inside. The commonest dropdown dialect on the web; deliberately edge-poor so only class or behaviour can find it.",
      "fields": {
        "trigger": "#auditDropdownBtn"
      },
      "hidden": true
    },
    {
      "type": "listbox",
      "root": "#auditSizeList",
      "why": "A replaced <select>: hidden native control, visible toggle whose TEXT CHANGES to the chosen option, options that select a value rather than navigate. The Molina pattern, pinned. Items are buttons, not links — a listbox, not a menu.",
      "fields": {
        "trigger": "#auditSizeToggle",
        "options": "#auditSizeList>li>button"
      },
      "hidden": true
    },
    {
      "type": "combobox",
      "root": "#auditModelBox",
      "why": "An autocomplete: the suggestion list is EMPTY until typing fills it — the commonest autocomplete shape, and the one the count-based probe rung exists for. role=combobox on the input in the friendly build only; hostile must name it from the typing.",
      "fields": {
        "textbox": "#auditModelInput",
        "listbox": "#auditModelList"
      }
    },
    {
      "type": "dialog",
      "root": "#chatWindow",
      "why": "The live-chat panel: fixed, floating over the page, opened by the FAB. A real openable component the corpus simply had no label for — it was counted as a false positive in every build until this entry (owner decision, 4.2 era: corpus change, not code).",
      "fields": {
        "trigger": "#chatToggle"
      },
      "hidden": true
    },
    {
      "type": "dialog",
      "root": "#auditLeaveDialog",
      "why": "A 420px fixed confirm box with a backdrop and NO role anywhere — the small dialog the overlay size test cannot see. Declared via data-panel-target on its trigger.",
      "fields": {
        "trigger": "#auditLeaveBtn"
      },
      "hidden": true
    },
    {
      "type": "accordion",
      "root": "#auditReadMore",
      "why": "A single disclosure: one trigger with aria-expanded, one adjacent panel of prose. Type accordion by decision, with single:true for the stage-4 subtype.",
      "single": true,
      "fields": {
        "contentSelector": "#auditReadMorePanel"
      }
    },

    {
      "type": "tooltip",
      "root": "#auditTip",
      "why": "role=tooltip, hidden until its control is described. The role was never in COMPONENT_BY_ROLE, so it rode in on the edges and then had no name.",
      "fields": {
        "trigger": "#auditTipBtn"
      },
      "hidden": true
    },
    {
      "type": "button",
      "root": "#auditFakeBtn",
      "why": "7.1: an <a href=\"#\"> whose click flips its own state (class + text) and never navigates — a button in link's clothing. The behavioural voice carries it: pressed, changed, navigated:false, nothing revealed. The dead-href markup arm was deliberately narrowed — this very page has 31 innocent href=\"#\" links.",
      "fields": {}
    },
    {
      "type": "button",
      "root": "#auditBareDiv",
      "why": "7.1: a bare div with tabindex=0 and a pointer cursor whose click flips its own state — a button with no role at all. tabindex on a non-control is also the markup arm's hint.",
      "fields": {}
    },
    {
      "type": "none",
      "root": "#auditBtnLink",
      "why": "7.1's negative: a REAL link (href=/sale) styled with the same btn classes as its neighbours. Styling must not make a working link a button — and a healthy link is not a component to publish."
    },
    {
      "type": "none",
      "root": "#auditSwitch",
      "why": "role=switch is a checkbox subtype — a PART. Its class says switch-nav, and 'nav' must not make the button a menu. Two rules pinned at once."
    },
    {
      "type": "none",
      "root": "#auditMenuToggle",
      "why": "A lone button whose CLASS says menu-toggle, outside any nav, opening nothing on this page. A part-name is not a component."
    },
    {
      "type": "none",
      "root": "#auditOpenModal",
      "why": "A lone button whose CLASS says open-modal. The class rule that reads 'modal' must not call the BUTTON a dialog."
    },
    {
      "type": "none",
      "root": "#auditPriceSlider",
      "why": "A price range control — input[type=range] in a wrapper whose class says slider. A slider is not a carousel."
    },
    {
      "type": "none",
      "root": "#auditGallery",
      "why": "A static image grid whose class says gallery. Nothing cycles, nothing hides — not a carousel."
    },
    {
      "type": "none",
      "root": "#auditLayoutTable",
      "why": "A 3×3 <table role=\"presentation\"> used for layout. The role is the author saying 'not a table'; detection must listen."
    },
    {
      "type": "none",
      "root": "#auditFilters",
      "why": "Three selects and a type=button 'Clear'. Fields plus a clickable is not a form — nothing here submits anywhere."
    },
    {
      "type": "none",
      "root": "#auditTabsContent",
      "why": "A div whose class says tabs-content, sitting inside a strip. The CONTENT of tabs is not the tabs."
    },
    {
      "type": "tabs",
      "root": "#auditMiniStrip",
      "why": "The mini strip AROUND the tabs-content decoy is itself a real two-tab strip — two sibling buttons swapping panels in place. The decoy pins what must not be flagged; this pins what must.",
      "fields": {}
    }
  ],
  "carousels_secondary": {
    "note": [
      "The four `.rail` strips are horizontal scrollers with prev/next buttons,",
      "and `.ticker` is an auto-advancing marquee. Whether each is 'a carousel' is",
      "a judgement call a specialist makes, so they are held apart from the scored",
      "set rather than counted as a hit or a false positive either way."
    ],
    "roots": [
      ".rail",
      ".ticker"
    ]
  }
}
